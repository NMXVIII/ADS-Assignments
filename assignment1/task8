def all_sequences(n, k, current):
    if len(current) == n:
        print(*current)
        return
    for i in range(1, k + 1):
        current.append(i)
        all_sequences(n, k, current)
        current.pop()

n, k = map(int, input().split())
all_sequences(n, k, [])
